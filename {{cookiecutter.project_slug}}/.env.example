# Only this variable is required in the production GitHub Environment.
COURSE_DOMAIN=course.example.edu
# Optional: an absolute directory on the Docker daemon host. Empty = named volume.
COURSE_DATA_ROOT=
# Optional local diagnostic port. Traefik connects directly to container port 80.
COURSE_PORT=8080
